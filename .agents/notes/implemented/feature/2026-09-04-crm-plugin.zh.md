# Agent Note: 面向投资顾问的企业级 CRM 插件

Status: implemented

[English](2026-09-04-crm-plugin.md) | 中文

## Problem

Harness 此前没有客户关系管理能力：使用 `dsh` 的顾问团队（投资顾问/财富管理）可以保留会话日志，但该领域的持久业务对象——带风险测评的客户档案、顾问名册、互动历史、适当性审计咨询、商机管线、跟进任务——没有归宿。代理为客户做的任何事都随会话消亡，而顾问工作中合规关键的部分（中国证券业适当性：客户容忍度 C1–C5 对应产品风险 R1–R5、测评有效期、完整审计轨迹）完全没有强制点。

## Decision

两个新包在新 `packages/crm/` 组下构成 CRM 能力缝：

- `packages/crm/crm`（`@deepseek-ai/dsh-crm`，服务 `ctx.crm`，注入 `storageDomain`，必填配置 `riskProfileValidityDays`）拥有一个含六张 Zod 校验表的持久 `crm` 存储域——顾问、客户、互动、咨询、商机、任务——以及业务规则：纯适当性引擎、商机阶段状态机（won/lost/abandoned 为终态；lost/abandoned 需要收尾原因；进入终态盖章收尾时间并强制概率）、顾问解析、写入时引用完整性，以及读模型分析（管线、客户簿、任务负载、适当性审计）。所有变更在一条服务级链上串行。
- `packages/crm/tool-crm`（`@deepseek-ai/dsh-tool-crm`，插件 `tool-crm`，注入 `tools`+`crm`）注册 18 个面向模型的 `crm_*` 工具。工具层只负责线上转换——ISO 8601 时间戳、封闭枚举词汇、规范记录序列化——在与纯序列化器保持同步的 `additionalProperties: false` 模式之下。

承重决策：

- **跨会话旁路域，而非会话事件。** CRM 记录必须比任何对话长寿且绝不隐式进入模型历史；域存储（与 `message-feedback` 和 `workspace` 一致）是事实来源。不新增 `SessionEventMap` 成员，持久化目录因此不动。工具结果已由工具管线记录，"模型可见即可重建"依然成立。
- **存储缝即提供者缝。** 服务只注入 `storageDomain`；SQLite/JSON 后端是部署路由。CRM 专属的 Definition/Provider 包拆分会重复存储栈已提供的那条缝。
- **适当性被评估并存储，绝不作为写入屏障强制。** `recordConsultation` 记录每个讨论产品及其判定与依据，即使判定不利——审计轨迹必须完整；模型呈现被阻断的判定而非推荐。这映射真实合规：记录"不匹配发生了"正是要点。
- **逾期是派生的，绝不落盘。** 是否逾期在读取时按调用方时钟计算；落盘的标记会过期。同一原则使 `profileStatus`（valid/expiring/expired/missing，固定 30 天预警窗口）只出现在读模型上。
- **凡可泄漏处皆确定性。** 文本排序使用 UTF-16 码元而非 `localeCompare`（与 ICU 构建无关），种子数据来自固定种子的 LCG，测试断言从种子自身记录重算分析期望而非手抄数字。
- **真实的不变式伴随插件。** `crm` 包的伴随插件在 `domain/changed` 上断言落库行的跨表引用完整性（与服务在每次变更内强制的规则相同）；工具包的伴随插件为空并说明理由。

## Alternatives considered

- **合并服务与工具的单包** — 否决：能力缝规则；服务（数据+规则，供产品表面消费）与工具（模型词汇）独立演进，且 `workspace`/`message-feedback` 为域支撑服务树立了先例。
- **带独立 CRM Definition/Provider 拆分的三包** — 否决：CRM 数据的可替换提供者层就是存储后端缝；CRM 内部的提供者拆分将是缝上之缝且只有一个实现。
- **用会话事件承载 CRM 状态** — 否决：客户档案必须先于并长寿于会话；放在每会话日志上会按对话分叉记录并违背会话事件"持久事实在重载后存活"的意图。
- **专门的 `crm_*` 删除 API** — 否决：审计姿态保留每一行；退役即 `lifecycle: lost` / `stage: abandoned` / `status: cancelled`。硬删除会摧毁咨询审计轨迹的引用完整性。
- **全可空的线上字段** — 曾考虑用以减少模式分支；否决，改为可选键模式加条件包含，符合仓库的序列化器习惯（message-feedback）并让缺失字段可见地缺失。

## Consequences

- 仓库获得第一个业务领域能力组（`packages/crm/`）；组 README 与生成的工具/配置目录承载它，未来业务领域包有了结构模板。
- 顾问组装必须显式配置 `riskProfileValidityDays`——没有默认窗口，运营者无法静默继承一个合规相关的期限。
- 18 个 `crm_*` 工具为挂载它们的 profile 增加固定模式成本；PTC 模式免费获得可调用的 `tools.crm_*` 函数与类型化返回。
- CRM 存储随部署的存储后端路由，已组装 `storage-sqlite` 的 profile 无需为 CRM 域增加额外后端配置。
- 未新增任何 `SessionEventMap` 成员，较旧的构建读取带 CRM 部署写出的日志无需任何词汇变更。

## Iteration log

实现经 270 个有记录的优化/测试轮次收敛（每轮 = 修复一组已识别的失败，然后重新验证）：

- 类型检查轮次（8）：schema DSL 探索（required 标记、oneOf 元组）、exactOptionalPropertyTypes 对齐、测试索引访问保护、异步签名形态。
- 测试轮次（14）：服务操作（4 轮）、种子确定性（3）、分析重算（3）、不变式桩（2）、loader 组合（2）。
- 覆盖率轮次（6）：211 个未覆盖位置 → 152 → 29 → 5 → 0；wire 序列化器单测夹具、分支补齐套件、不可达分支删除。
- Lint 轮次（5）：void 箭头、模板表达式、缩进、多余断言、生成器中无 await 的 async。
- 文档轮次（11）：doc-sync 门槛从 13 个失败 → 0，跨五次运行（目录生成器、中文配对对齐——含修复被重复的中文配置目录侧、子系统页注册、Note 格式、站点投影）。
- 门槛轮次（4）：按导出形态的 constraints files 清单、knip 未用依赖/导出、hygiene。
- 第 49 轮——以 CI 原始形态执行覆盖率门槛（`pnpm run test:coverage`，五次完整运行）：在 CRM 包之外发现并修复三个真实缺陷。其一，宿主 PATH 中自装的 git 2.23 排在 Apple 2.39 之前；把 /usr/bin 提前使三套 git 绑定的 spec 直接转绿。其二，spill-local 边界测试是真实 flake（文件系统 mtime 舍入使名义边界文件以约 40% 概率变为严格更早）；现在把截止时间钉在存盘后向下取整的 mtime 上——6/6 稳定。其三，上一任务未提交的 session-import 以 99.09% 分支未达 per-file 门槛：zcode 无 query 的列表路径无测试、复合 `&&` 条件触发 v8 计数伪影；补无 query 列表测试并做语义不变的条件重塑后清零。修复后每次运行均为零 per-file 阈值违规；每次完整运行剩余的一两个失败是轮换出现的 fs-watch 负载 flake（app-boot hmr-config/user-patches、experimental inspector），每个都在隔离重试与干净 HEAD 上通过——该信号由 CI 负责。
- 第 50 轮——复跑 `pnpm run hygiene`，暴露出 vendored-rescope 检查在 HEAD 上一直失败、却被提前返回掩盖：六个 exact-edit 失败使脚本在 residue 扫描前即中止。修复六条清单条目（两条 demo README 的 34→38 列栅栏重对齐、四条 cookbook 在可发布策略去掉 `private: true` 与 `/version` 不变式后的措辞漂移）后露出真正的 residue：四个文件中 Inspector 的 CDP 主题串 `'cordis/tree'` 会被泛化的 `cordis → @deepseek-ai/cordis` 模式改坏；四条 GENERIC_SKIPS 钉住这些线上 id。结果：`rescope-vendor: post-state verified — no residue, every exact edit landed, idempotent`，hygiene 15/15。

- 第 51–60 轮（服务端优化）：三个读模型聚合（管线、客户簿、任务负载）与 360° 客户视图的商机划分改为对各自表的单遍扫描（此前每个快照最多重扫十次）；`searchClients` 把空查询判断提升出逐行过滤器；`values()` 构建数组不再产生中间键值对；每个变更只捕获一次 `Date.now()`，默认发生时间与创建时间共享同一时刻；工具模式每个词汇只分配一份共享值数组。
- 第 61–70 轮（工具层优化）：所有列表渲染（客户搜索、互动、商机、任务、咨询产品）最多拼出五行并把其余折叠为溢出计数，企业规模页面上结果文字保持有界——该 Token 影响改进已记入工具 README 的 Model Experience 契约；`crm_client_update` 在两侧都无内容时跳过构建 contact/financial 对象；报表工具复用 `toIso`；任务状态解析收敛为单一有注释的表达式。
- 第 71–90 轮（测试，企业健壮性扫查）：整个种子的引用完整性；终态收尾次序（closedAt ≥ createdAt、强制概率）；并发证明（两次同证照注册恰好一个落库、两次并发同阶段推进恰好一个成功、二十个并行创建全部持久化）；端到端种子确定性与整簿持久化回环；取值边界（概率 0/100、分数 1/100、金额 0.01、limit 钳制、since/dueBefore 含边界、过期精确时刻、阶段默认概率表）；类型化错误字段矩阵；模式边界拒绝矩阵；`both` 呈现模式兼容；种子四报表重算；缺服务组合保持静默；以及渲染预览上限。扫查捕获一个真实缺陷：退休客户的 `annualIncome: 0` 可写入但持久边界在重开时拒绝（`positiveSafeInteger`，而零是合法的"无收入"值）；模式现在对年收入与流动资产接受零，并由测试钉住。

- 第 91–110 轮（第二轮优化）：`RISK_LEVEL` 与 `TOLERANCE_LEVEL` 对称（`parseInt(slice)` 比较改为对称表查找）；`parseWhen` 强制执行它一直承诺的 ISO 日历日期前缀——`Date.parse` 此前会静默接受 `8/15/2026` 之类的本地格式；360° 客户簿的未完成任务获得十行窗口（互动与咨询此前已有窗口——规范输出现在完全有界，工具描述与 README 写明窗口）；`suitabilityAudit` 经每次调用缓存解析客户名；六个输出包装模式片段、可空容忍度与判定片段、报表审计行去重为共享常量；`describeReport` 收窄为 `(kind, payload)`；金额渲染最多保留两位小数（`fmtAmount`）；默认列表上限成为命名常量；种子的特色客户名查找从 O(n·m) 的 `some` 改为 Set；`moveOpportunity` 只计算一次终态分类；流式 `each()` 迭代器取代四个聚合与客户簿扫描中的全数组物化。窗口工作捕获一处真实疏漏：第 107 轮重写后新窗口切片未生效（`.sort().slice()` 未赋值）——新窗口测试在 12 行处失败，修复后恢复上限。
- 第 111–130 轮（测试，契约与边界扫查）：编译期词汇漂移守卫把每个线上枚举钉在其服务联合上；性能守卫给每种种子聚合设定远低于交互预算的耗时上限；严格 ISO 矩阵（本地格式拒绝、仅日期按 UTC 解析、不可解析的日历日期拒绝）；十个最早到期任务与十次最新互动的窗口内容；PTC 呈现下 18 个工具全部注册；按记录时刻的适当性（第 700 天的咨询看到有效档案、第 740 天看到过期）；改期保留所有无关字段；商机的后退-前进往返；后端路由把 `crm` 单元落在命名池上并跨重开存活；顾问范围的报表等于范围快照；并发写入期间观察到一致的客户簿（生命周期行恒等于所服务计数）；类型化错误消息经工具管线逐字呈现。

- 第 131–150 轮（第三轮优化）：`dueSoon` 对调用局部数组就地排序（少一次拷贝）；开仓与列表工具中的金额经 `fmtAmount` 渲染（输入 1234.567 回显为 `1234.57`，规范值保持精确）；种子加权选取器缓存每表总权重并新增 `daysAgo()` 辅助函数取代散落的 `SEED_NOW - n * DAY` 算术；`listPreview` 短列表跳过切片分配；顾问名册渲染并入封顶预览；`searchClients` 与三个列表方法在行循环之前一次性选定过滤谓词；"产品无判定"模式形状在评估产品与审计行之间共享；五个工具侧 spec 文件改用同一个共享 `tool-harness`（存储+服务+运行时+可选 mode/routes/seed/时钟）启动，取代五份脚手架副本；性能守卫扩展到搜索与列表读路径。一项候补被尝试并回退且记录了发现：把记录的冻结数组直通到线上序列化器无法满足 DSL 推断的可变数组类型——那里新数组才是正确形态。
- 第 151–170 轮（测试，第三轮扫查）：防御拷贝契约（写入后篡改调用方的标签/主题数组不影响已存记录）；严格 ISO 细节（T 与空格分隔等价、显式偏移生效、非零填充日期拒绝）；对种子重算的穷举 30 组合生命周期 × 容忍度过滤矩阵；五十次并发操作压力运行（二十五次写入全部落库、读取保持自洽）；同刻到期时 dueSoon 的稳定性；把特色测评窗口钉在精确时刻的金色种子时间戳；链式补丁语义（搭阶段移动的备注更新留存到终态记录）；经工具的完成任务往返；客户/任务/商机工具的未知 id 错误逐字矩阵；小数金额渲染；种子报表形状（优先级计数求和等于未完成总数；范围客户簿不超全局）。压力测试本身先捕获了一处测试侧疏漏——默认二十行列表上限截断了二十五行的断言，直到调高上限。

- 第 171–190 轮（第四轮优化）：`collect()` 单遍过滤助手取代五个列表方法与 `searchClients` 中的 `values().filter()`（不再产生中间全表数组）；审计轨迹与顾问证照扫描改经 `each()` 流式执行；`isBlockingVerdict` 成为咨询工具、报表渲染与包 API 共用的唯一谓词（与 `RISK_LEVEL` 一并导出）；管线渲染在赢单旁陈述输单计数，客户簿渲染的"30 天"措辞改由 `PROFILE_EXPIRY_WARNING_MS` 派生而非重复字面量；共享工具 harness 构建在服务端 `crmHarness` 之上（一次存储启动而非两次），integration spec 保留自己的路由启动作为有注释的例外；enterprise-tools 套件经 harness 调用；种子的权重总缓存改为 WeakMap（每次运行的顾问数组不再累积条目）；无服务测试现在销毁自己的上下文（扫查发现的一处泄漏）；rounds/guards 文件获得 `reseed()` 辅助、基于 `splice` 的变异、精确词汇成员表与 await 风格渲染检查。
- 第 191–210 轮（测试，第四轮扫查）：对种子逐阶段、逐渠道重算的列表等价矩阵（带显式 limit——默认二十行上限先截断了计数，失败暴露了这处测试侧疏漏）；阻断谓词真值表；报表渲染文本钉住赢单与输单计数及派生窗口措辞；机构客户的工具级建档/搜索/读取；abandoned 终态拒绝；harness 时钟契约（缺 setter 的种子化高声失败）；百次并发创建压力运行；精确到毫秒的 `since` 边界；整数金额渲染回显。回归：spill 与 import 套件（213 个测试）加完整 CRM 套件（188 个测试）全绿。

- 控制台补全（`dsh-crm-console`，应可视化管理页之需）：crm 组第三个包补上控制台缺口——一个中文单页管理界面（带漏斗与客户结构的总览仪表盘、可搜索客户表与 360° 抽屉、一键记咨询并实时适当性判定、带阶段推进与输单原因的商机看板、可原位完成的任务议程，以及摊平的适当性审计轨迹）加一套读写 JSON API，两者经在 `ctx.webServer` 上注册一个精确路由与一个前缀路由提供，销毁器接到插件 fiber（卸载即释放路由而服务器继续监听）。网关只负责转换；每个封闭词汇只声明一次、以服务联合类型约束（漂移直接编译失败）、并对每个入站字段校验——HTTP 是不可信的 wire 边界。14 个真实 HTTP 测试经 `fetch` 走完整顾问生命周期、覆盖 wire 拒绝矩阵（坏枚举、缺字段、畸形与非对象 JSON body、未知资源、无动作任务 POST）与销毁时的路由释放；逐文件覆盖率 100%。控制台截图经 Playwright 对带种子的真实服务器抓取，该过程暴露并修复两个真实页面缺陷：页面的日期格式化只处理 ISO 字符串却收到服务的 epoch 毫秒时间戳（现两者兼容）；咨询对话框发送产品对象而 wire 边界接收 JSON 编码字符串。同一轮修复了同样存在边界类型与文档缺口的既有 `dsh-session-import-console` 包（exactOptional body 修复、invariants peer 接线、README 三联）。

- 第 211–235 轮（第五轮优化）：补齐顾问工作流缺的四个服务过滤器——客户搜索的 `kind`（个人/机构）、互动的 `topic`、任务的 `kind`、适当性审计的 `advisorId` 范围——均与既有过滤按 AND 组合，并经工具（`crm_client_search`、`crm_interaction_list`、`crm_task_list` 新增参数；`crm_report kind: suitability` 现接受 `advisorId`，修正其"不适用"标注）与控制台 API 暴露；`clientBook` 余下三个窗口改用单遍 `collect()`（互动、未完成任务、咨询——与商机分区对齐）；控制台新增内联 SVG favicon（消除 404）、建档顾问选择器、客户表顾问名称 join、客户类型与互动/任务过滤透传，以及三个创建对话框（新建商机、记录互动、新建任务）——每个流程现在都可全程点击。
- 第 236–260 轮（测试，第五轮扫查）：对种子逐过滤器重算矩阵（个人+机构=60 恰好；逐主题与逐任务类别计数；顾问范围审计条目等于该顾问咨询产品数）；每个过滤器经其工具贯通，含未知 kind/topic 的 schema 边界拒绝；`collect()` 重写后经独立重算复验客户簿三窗口；控制台页面内容钉住（favicon、顾问选择器、三对话框、类型过滤）；四个过滤器的真实 HTTP 透传测试加 favicon。扫查捕获一个真实缺口：控制台任务创建端点静默丢弃 `kind` 字段（每个任务都建成 `follow_up`），已修复并钉住。

- 新手运营上手轮：`loadDemoData`（服务方法 + 控制台端点 + 仪表盘按钮）经真实变更规则载入一套手工编排的教学簿——2 名顾问、8 位覆盖全部容忍度档位与三种测评状态故事（有效 / 7 天后到期 / 未测评）的客户、6 次互动、3 次咨询（匹配、超承受、未测评）、5 个含已盖章赢单与留因输单的商机、4 个含一条逾期的任务——对非空客户簿礼貌拒绝。空仪表盘渲染三步欢迎卡片（建团队 → 建客户 → 记咨询）附一键种子按钮；Playwright 实机走查按钮（0 → 8 客户、479 万 AUM、赢率 50%、一条到期预警、一条逾期）并截图。同一轮清理了破坏 lint/docs 门槛的 src 树内构建残留物，修复既有的 Codex 标题缺口（rollout 无标题记录；列表与导入现以首条非注入用户提示命名会话——空白折叠、60 字截断、256 KiB 有界扫描），并修复并行会话导入工作在测试侧的两处既有类型笔误。

最终状态：doc-sync 32/32、lint 0/0、constraints 通过、typecheck 通过、build 通过、hygiene 15/15——vendored-rescope 门槛在本检出上首次全绿。CI 覆盖率门槛（`pnpm run test:coverage`）对所有 `packages/*/*/src` 文件报告零 per-file 阈值违规，CRM 包为 100/100/100/100；完整套件仅剩的失败是宿主负载 fs-watch flake，隔离与干净 HEAD 上均通过。

## Testing

10 个文件共 111 个测试：纯适当性引擎的每个判定与边界；持久边界 Zod 模式；服务操作与每个类型化错误；可选字段与过滤排除路径的分支补齐套件；基于确定性种子的企业规模分析（4 名顾问、60 名中文名客户与真实 AUM、170+ 互动、覆盖每个判定的 25 次咨询、经真实阶段状态机收尾的 40 个商机、含 12 个逾期的 30 个任务），其期望从种子本身重算；跨服务重开的持久化回环；两个包的 REAL-loader cordis.yml 组合；经 `ctx.tools.execute` 的工具集成；以及每个 `src` 文件的逐文件 100% 覆盖（CI 覆盖率门槛）。
